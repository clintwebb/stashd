
stashd requires several libraries to be installed before stash can be built/executed.

These are:

 - libstash
 - liblinklist
 - libexpbuf
 - librispbuf
 - librisp
 - libevent
 - libexpbufpool

When the required libraries have been installed, build everything by doing

  make

and if successfull, install with

  sudo make install

